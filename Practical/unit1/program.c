#include<stdio.h>
int main()
{
    int b,h,a;
    printf("enter the base of a triangle:");
    scanf("%d",&b);
    printf("enter the hgeight of a ttraingle:");
    scanf("%d",&h);
    a=(((b*h)*1)/2);
    printf("%d",a);
    return 0;
}
