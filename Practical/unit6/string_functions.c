#include <stdio.h>
#include <string.h>
int main()
{

    char str[100] =  "mridul ";
    char str2[100] = "kumar mondal";

    // printf("%s\n", str);

    // strcat(str, str2); //String concatanation

    // printf("%s", str);

    // printf("%s\n", str);

    // strncat(str,str2,5); //String concatanation upto n

    // printf("%s", str);

    // int n=strlen(str);
    // printf("%d",n); //For finding length

    //  strcpy(str,str2); //For copying string

    //  printf("%s",str);

    return 0;
}